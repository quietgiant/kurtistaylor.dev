{
  "name": "kurtistaylor.dev",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npx tailwindcss -i ./src/input.css -o ./public/styles.css --watch\" \"http-server ./public -p 3000\"",
    "build": "npx tailwindcss -i ./src/input.css -o ./public/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "http-server": "^14.1.1",
    "concurrently": "^8.2.2"
  }
}